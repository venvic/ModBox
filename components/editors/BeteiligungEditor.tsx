'use client'


const BeteiligungEditor = ({ id, productId, onChangesSaved }: { id: string, productId: string, onChangesSaved: () => void }) => {
    return (
        <div className="flex flex-col w-full h-full border">
            
        </div>
    )
}

export default BeteiligungEditor;